// Web3Forms public access key (NOT a secret — safe to commit).
// Get it free at https://web3forms.com using info@hikasustravel.com, then paste it below.
// While this is empty, all forms fall back to a "email us" notice, so the live site is never broken.
export const WEB3FORMS_KEY = 'bc08abb5-7b55-4df5-aee3-b5e2d2d6a977'

// Optional Cloudflare Turnstile site key (anti-spam). Empty = Turnstile disabled (honeypot still active).
// The matching Turnstile SECRET key is configured in the Web3Forms dashboard, not here.
export const TURNSTILE_SITEKEY = ''

// Where the inbox notice / mailto fallback points.
export const CONTACT_EMAIL = 'info@hikasustravel.com'
